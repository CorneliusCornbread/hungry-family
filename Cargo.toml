[package]
name = "hungry-family"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
sqlx = "0.8.6"
tokio = { version = "1.5.0", features = [
    "rt-multi-thread",
    "macros",
    "net",
    "time",
    "io-util",
    "sync",
    "fs",
] }
